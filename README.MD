# AgriGuard / Plant Animal Recognition Project

Computer vision project for animal and plant recognition.

## Setup

```bash
python -m venv venv
venv\Scripts\activate
python -m pip install -r requirements.txtgit status